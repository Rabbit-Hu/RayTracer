#pragma once
#include <cmath>

static const double EPS = 1e-6;
static const double INF_D = 1e9;
static const double REFLECT_LIMIT = 5; // max ray-trace depth
static const double PI = acos(-1);
static const double DISPERSION_SAMPLES = 5;